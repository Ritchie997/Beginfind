node_modules/
*.db
*.db.backup
.env
.env.local
logs/
*.log
npm-debug.log*
.DS_Store
*.pem
*.key
/public/uploads/*
!/public/uploads/.gitkeep
temp/
tmp/
.qwen
backups/
backup-settings.json

# Статьи (Markdown) — реальный пользовательский контент, не версионируется,
# как раньше articles.db. Живёт только на диске сервера; резервные копии —
# через /api/backups (см. src/services/backup.js).
/content/